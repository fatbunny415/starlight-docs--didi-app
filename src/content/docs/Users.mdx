
---
title: Usuarios
description: Microservicio para la gestión de usuarios del sistema
---

import { Aside } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';

### Microservicio de Usuarios

<Aside type="caution" title="IMPORTANTE">
Este servicio permite administrar los usuarios del sistema: creación, consulta, actualización y eliminación.  
Incluye validaciones, manejo de errores y persistencia con SQLAlchemy.
</Aside>

El microservicio de **Usuarios** expone endpoints para realizar operaciones CRUD sobre los usuarios registrados.  
Se integra con otros servicios mediante FastAPI y utiliza una base de datos relacional.

#### utores: Daniel Alejandro Cardona Rico y Simon Alzate Velasquez  
#### Fecha: 2025-11-13  
#### Versión: 1.0.0  
#### Estado: Desplegado en Producción

---

## Crear Usuario

**endpoint** <Badge text="POST" size="large" variant="caution" />
```
/api/v1/users
```

**Request**
```json
{
  "name": "Nombre del usuario",
  "email": "usuario@correo.com",
  "password": "********",
  "role": "user"
}
```

**Response** <Badge text="201" variant="success" />
```json
{
  "id": 1,
  "name": "Nombre del usuario",
  "email": "usuario@correo.com",
  "role": "user"
}
```

<Badge text="400" variant="danger" />
```json
{
  "detail": "Error al crear usuario: [detalle técnico]"
}
```

<Badge text="500" variant="danger" />
```json
{
  "detail": "Error interno del servidor: [detalle técnico]"
}
```

---

## Listar Usuarios

**endpoint** <Badge text="GET" size="large" variant="caution" />
```
/api/v1/users
```

**Response** <Badge text="200" variant="success" />
```json
[
  {
    "id": 1,
    "name": "Nombre del usuario",
    "email": "usuario@correo.com",
    "role": "user"
  }
]
```

<Badge text="500" variant="danger" />
```json
{
  "detail": "Error interno del servidor: [detalle técnico]"
}
```

---

## Consultar Usuario por ID

**endpoint** <Badge text="GET" size="large" variant="caution" />
```
/api/v1/users/{user_id}
```

**Response** <Badge text="200" variant="success" />
```json
{
  "id": 1,
  "name": "Nombre del usuario",
  "email": "usuario@correo.com",
  "role": "user"
}
```

<Badge text="404" variant="danger" />
```json
{
  "detail": "Usuario no encontrado"
}
```

<Badge text="500" variant="danger" />
```json
{
  "detail": "Error interno del servidor: [detalle técnico]"
}
```

---

## Actualizar Usuario

**endpoint** <Badge text="PUT" size="large" variant="caution" />
```
/api/v1/users/{user_id}
```

**Request**
```json
{
  "name": "Nuevo nombre",
  "email": "nuevo@correo.com",
  "role": "admin"
}
```

**Response** <Badge text="200" variant="success" />
```json
{
  "id": 1,
  "name": "Nuevo nombre",
  "email": "nuevo@correo.com",
  "role": "admin"
}
```

<Badge text="400" variant="danger" />
```json
{
  "detail": "Error al actualizar usuario: [detalle técnico]"
}
```

<Badge text="404" variant="danger" />
```json
{
  "detail": "Usuario no encontrado"
}
```

<Badge text="500" variant="danger" />
```json
{
  "detail": "Error interno del servidor: [detalle técnico]"
}
```

---

## Eliminar Usuario

**endpoint** <Badge text="DELETE" size="large" variant="caution" />
```
/api/v1/users/{user_id}
```

**Response** <Badge text="200" variant="success" />
```json
{
  "message": "Usuario eliminado correctamente"
}
```

<Badge text="404" variant="danger" />
```json
{
  "detail": "Usuario no encontrado"
}
```

<Badge text="500" variant="danger" />
```json
{
  "detail": "Error interno del servidor: [detalle técnico]"
}
```
```

